# Steps to prepare us to build various tools in this directory in various languages
# (i.e. not Python or another language that I decide merits its own directory).
# This is a fairly scattershot selection of items i think would be fun to build
# from source.
# At present it's only this one repo, and git should already be installed and 
# configured by now as a bootstrap

pushd ~
    git clone https://github.com/bazelbuild/bazel.git
popd
